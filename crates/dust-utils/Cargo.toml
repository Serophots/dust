[package]
name = "dust-utils"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/utils.rs"

[dependencies]
# Leaf crate - depend on no other crates in /crates

string-interner.workspace = true
bumpalo.workspace = true

miette.workspace = true
camino.workspace = true

derive_generic_visitor.workspace = true

serde.workspace = true
